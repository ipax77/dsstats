@page "/stats"
@using dsstats.shared
@attribute [RenderModeServer(prerender: true)]

<dsstats.razorlib.Stats.StatsComponent Request="Request" />

@code {
    private StatsRequest Request = new()
        {
            TimePeriod = TimePeriod.Past90Days,
            RatingType = RatingType.Cmdr
        };
}
