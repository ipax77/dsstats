@page "/stats/timeline"
@using pax.dsstats.shared;
@layout StatsLayout

<sc2dsstats.razorlib.Stats.Timeline.TimelineComponent Request="Request"></sc2dsstats.razorlib.Stats.Timeline.TimelineComponent>

@code {
    TimelineRequest Request = new()
    {
        TimePeriod = TimePeriod.Last2Years,
        RatingType = RatingType.Cmdr
    };
}
