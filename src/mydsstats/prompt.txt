I'd like to activate the navigation arrows in the dsstats.pwa/Pages/Home.razor ReplayComponent to be able to load the next/previous replay
I'd also like to add a SessionProgress simulation as we do here: /home/pax77/git/dsstats/src/maui/dsstats.maui/Components/Pages/Home.razor
Since we don't have a dedicated Session in the PWA app, we need to find some reasonable time/number based approach. Maybe let the user switch/select a time window 3h - 24h or number window 10-50
We have to respect the get ratings endpoints ratelimit /home/pax77/git/dsstats/src/server/dsstats.api/Controllers/ReplaysController.cs:
    [EnableRateLimiting("fixed")] [HttpGet("rating/{replayHash}")] =>
    builder.Services.AddRateLimiter(options =>
    {
        options.AddFixedWindowLimiter(policyName: "fixed", options =>
        {
            options.PermitLimit = 4;
            options.Window = TimeSpan.FromSeconds(12);
            options.QueueProcessingOrder = QueueProcessingOrder.OldestFirst;
            options.QueueLimit = 2;
        });
    }
Session progress is only available if replays are uploaded so the UploadCredential has to be true
Also, in the PWA app, we don't know who the player is to track, so we need a logic to determine and store the player profile(s) (toonIds).
We could add a MyProfiles page and let the user select them in addition of tracking the last 10 replays and define the profile with the most occurences as a player profile.